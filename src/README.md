# RootAccess src scaffold

Task 1 foundation directories:
- `core/` for boot/runtime infrastructure
- `components/` for desktop/app UI modules
- `styles/` for split CSS in later phases

Current runtime entry remains root-level `index.html`, `main.css`, and `main.js`.
